GPTONROIDS_API_KEY=your_secret_api_key_here
GITHUB_TOKEN=your_github_personal_access_token_here
# WORK_DIR=/path/to/your/work_directory (optional, defaults to project_root/work)
# Comma-separated list of safe commands allowed to be executed.
# If empty or not set, defaults to: ls,pwd,uname,echo,cat,hostname,git
# Example with additional safe commands:
SAFE_COMMANDS=ls,pwd,uname,echo,cat,hostname,git,grep,tail,mkdir,rm,sh,bash,sleep,adb,flutter,compile_and_run.sh,./compile_and_run.sh
PORT_TO_TUNNEL=8000 # Port for ngrok to tunnel, default 8000
